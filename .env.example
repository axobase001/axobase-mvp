# ================================================================
# Axobase MVP — 环境变量配置模板
# 复制本文件为 .env 并填入你的密钥
# cp .env.example .env
# ================================================================

# ── LLM 配置（二选一）────────────────────────────────────────────

# 方案 A: Kimi (moonshot) — 国内推荐
# https://platform.moonshot.cn 获取 API Key
LLM_PROVIDER=kimi
KIMI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
KIMI_MODEL=moonshot-v1-8k

# 方案 B: OpenRouter（支持 qwen、llama 等模型）
# https://openrouter.ai 获取 API Key
# LLM_PROVIDER=openrouter
# OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# OPENROUTER_MODEL=qwen/qwen-2.5-7b-instruct

# ── Agent 初始参数 ────────────────────────────────────────────────
INITIAL_AGENT_COUNT=10          # 初始 agent 数量（建议 8-15）
INITIAL_USDC_PER_AGENT=30       # 每个 agent 起始余额（USDC）

# ── 节奏控制 ─────────────────────────────────────────────────────
LLM_CALLS_PER_TICK=1            # 每 tick 调用 LLM 次数
MIN_LLM_INTERVAL_MS=1000        # LLM 调用最小间隔（ms）
TICK_INTERVAL_MS=10000          # 每 tick 时长（ms），建议 10000-30000
SNAPSHOT_INTERVAL_MS=120000     # 快照保存间隔（ms）

# ── 链配置（MVP 阶段不实际上链，仅用于地址生成）──────────────────
CHAIN=base-sepolia
BASE_RPC_URL=https://sepolia.base.org
MASTER_WALLET_PRIVATE_KEY=      # 可留空（MVP 不需要真实私钥）

# ── Arweave（可选，MVP 默认关闭）────────────────────────────────
ENABLE_ARWEAVE=false
IRYS_PRIVATE_KEY=

# ── 日志 ─────────────────────────────────────────────────────────
LOG_LEVEL=info
