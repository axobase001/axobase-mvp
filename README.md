# Axobase MVP — AI Agent 进化实验平台

> **版本**: V2 (Evolution Pressure)
> **状态**: 活跃开发中
> **最后更新**: 2026-02-24

Axobase 是一个 AI Agent 自主进化模拟平台。多个 agent 在资源约束下竞争、繁殖、死亡，观察涌现行为与种群动力学。

---

## 🚀 快速开始

### 环境要求
- Node.js >= 18
- Kimi API Key（[platform.moonshot.cn](https://platform.moonshot.cn)）或 OpenRouter API Key（[openrouter.ai](https://openrouter.ai)）

### 安装与运行

```bash
# 1. 克隆仓库
git clone https://github.com/axobase001/axobase-mvp.git
cd axobase-mvp

# 2. 安装依赖
npm install

# 3. 配置环境变量
cp .env.example .env
# 编辑 .env，填入你的 API Key（必填：KIMI_API_KEY 或 OPENROUTER_API_KEY）

# 4. 启动实验
npx tsx src/index.ts

# 5. 停止实验（Ctrl+C 会自动保存日志和终止报告）
```

---

## ⚙️ 关键配置（`.env`）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `KIMI_API_KEY` | — | Kimi API 密钥（必填之一）|
| `OPENROUTER_API_KEY` | — | OpenRouter 密钥（必填之一）|
| `INITIAL_AGENT_COUNT` | `10` | 初始 agent 数量 |
| `INITIAL_USDC_PER_AGENT` | `30` | 每个 agent 起始余额 |
| `TICK_INTERVAL_MS` | `10000` | 每 tick 时长（ms），10000 = 10秒 |

---

## 🏁 实验终止条件

| 条件 | 触发规则 |
|------|---------|
| **[A] 血统垄断** | 单一创始者后代 ≥ 70% 存活种群（种群 > 30）|
| **[B] 经济垄断** | 单个 agent 持有 ≥ 80% 总余额（种群 > 10）|
| **[C] 超长存活** | 存活 > 前5代平均寿命 × 5（≥10 样本）|
| **[D] 涌现行为** | 累计检测到 10 次涌现行为 |

---

## 🧬 核心机制（V2 进化压力）

| 参数 | 值 | 说明 |
|------|-----|------|
| 基础代谢 | $0.8/tick | 无收入约 37 tick 死亡 |
| 繁殖门槛 | $15 | 双方各扣 $5，子代获 $6 |
| 种群上限 | 30 | 超过 25 触发竞争淘汰 |
| 衰老开始 | tick 500 | 5% 基础死亡率，越老越高 |
| 环境事件 | 每 tick | 崩盘 5%、爆发 3%、瘟疫 2% |

---

## 📁 项目结构

```
axobase-mvp/
├── src/
│   ├── index.ts                  # 实验入口
│   ├── config/
│   │   ├── constants.ts          # 核心参数（终止条件、繁殖、死亡等）
│   │   └── env.ts                # 环境变量读取
│   ├── runtime/
│   │   ├── population.ts         # 种群管理、终止条件检查
│   │   ├── agent.ts              # Agent 类
│   │   └── conversation-logger.ts # 日志写入
│   ├── lifecycle/
│   │   ├── birth.ts              # 创建 agent
│   │   ├── breeding.ts           # 繁殖系统
│   │   ├── death.ts              # 死亡判定
│   │   ├── development.ts        # 发育阶段 + 衰老
│   │   └── survival.ts           # 每 tick 生存循环（9阶段）
│   ├── decision/                 # LLM 决策系统
│   ├── genome/                   # 动态基因组（63基因×8染色体）
│   ├── environment/              # 市场事件、任务、DeFi
│   ├── monitoring/               # 数字墓地
│   └── tools/                    # 钱包、网络、内存日志
├── .env.example                  # 配置模板（复制为 .env）
├── package.json
└── tsconfig.json
```

---

## 📊 日志输出

实验运行时自动写入 `./logs/`：

| 文件 | 内容 |
|------|------|
| `conversations.txt` | 每次 LLM 对话完整记录 |
| `offspring.txt` | 每个子代出生记录（含基因表达）|
| `tombstones.txt` | 每个死亡 agent 的墓碑档案 |
| `events.txt` | 环境事件 + 涌现行为 |
| `memory-{id}.txt` | 每个 agent 的思维日志 |
| `run-metadata-*.txt` | 实验配置快照 |
| `termination-*.txt` | 终止报告 |

---

## 🔗 链接

- GitHub: https://github.com/axobase001/axobase-mvp
- License: MIT
